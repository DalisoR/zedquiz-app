import { render, screen } from '@testing-library/react';
import React from 'react';

import { StudentBookingsPage } from './StudentBookingsPage';

describe('StudentBookingsPage', () => {
  it('renders correctly', () => {
    render(<StudentBookingsPage />);
    // Add assertions
  });
});
