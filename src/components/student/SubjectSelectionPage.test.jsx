import { render, screen } from '@testing-library/react';
import React from 'react';

import { SubjectSelectionPage } from './SubjectSelectionPage';

describe('SubjectSelectionPage', () => {
  it('renders correctly', () => {
    render(<SubjectSelectionPage />);
    // Add assertions
  });
});
