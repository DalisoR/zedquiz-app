import { render, screen } from '@testing-library/react';
import React from 'react';

import { TutorApplicationPage } from './TutorApplicationPage';

describe('TutorApplicationPage', () => {
  it('renders correctly', () => {
    render(<TutorApplicationPage />);
    // Add assertions
  });
});
